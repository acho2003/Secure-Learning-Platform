# 🔒 Security: Environment variables for sensitive data.
# This prevents hardcoding secrets into the application code.

PORT=5000
# 🔒 Security: Use a strong, unique connection string for your production database.
MONGO_URI=mongodb+srv://acho2003:ChenchoWangdi@chatlog.uptnxkh.mongodb.net/?retryWrites=true&w=majority&appName=chatlog

# 🔒 Security: Use a long, complex, and random string for your JWT secret.
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="YOUR_ULTRA_STRONG_AND_SECRET_KEY_HERE"
JWT_EXPIRES_IN="1h"

# 🔒 Security: Define the origin URL for your frontend application to enforce CORS.
FRONTEND_URL="http://localhost:3000"