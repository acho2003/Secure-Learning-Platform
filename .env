# .env (in the frontend project)

# 🔒 Security: Use an environment variable for the API URL.
# This prevents hardcoding the URL and makes it easy to switch between
# development and production environments.
# Note: React environment variables must start with REACT_APP_.
REACT_APP_API_URL=http://localhost:5000
VITE_API_URL=http://localhost:5000